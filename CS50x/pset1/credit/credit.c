#include <stdio.h>
#include <cs50.h>

int main(void)
{
    long card = get_long("Number: "), temp_card;

    temp_card = card;

    int mult = 0, do_not_mult = 0, i = 1;

    while (temp_card > 0)
    {

    }

}